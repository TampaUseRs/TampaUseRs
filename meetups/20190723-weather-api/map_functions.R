library(leaflet)

forecast.map <- function(forecast, title, subtitle) {
  
}

current.conditions.map <- function(observations, title, subtitle) {
  
}

cumulative.conditions.map <- function(observations, title, subtitle) {
  
}

